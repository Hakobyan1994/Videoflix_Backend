setup:
  addons:
    - plan: heroku-redis:mini
build:
  docker:
    web: Dockerfile.web
    worker: Dockerfile.web
run:
  web: sh -lc "python manage.py migrate --noinput && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"
  worker: python manage.py rqworker default